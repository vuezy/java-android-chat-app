# Java Android Chat App
An Android app built with Java for chatting and sharing posts.\
Since this project uses Firebase, the Firebase config file which is `google-services.json` must be added to the project.